<!DOCTYPE html>
<html>

<head>
    <title>Bertie's Books – List</title>
</head>

<body>
    <h1>
        <%= shopData.shopName %> — All Books
    </h1>

    <ul>
        <% availableBooks.forEach(function(book) { %>
            <li>
                <strong>
                    <%= book.name %>
                </strong> — £<%= book.price %>
            </li>
            <% }) %>
    </ul>

    <p><a href="/">Back to homepage</a></p>
</body>

</html>